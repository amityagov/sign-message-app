export type KeyType = {
  id: string;
  name: string;
  key: string;
  dateCreated: number;
};

export type KeyStatsData = {
  used: number;
};

export type KeyStats = {
  id: string;
  used: number;
};